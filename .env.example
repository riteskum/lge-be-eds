# Copy this file to `.env` and paste your Figma personal access token.
# Get one at: Figma → Settings → Security → Personal access tokens
# Never commit `.env` (it is listed in .gitignore).

FIGMA_TOKEN=
