.hlx/*
coverage/*
logs/*
node_modules/*

helix-importer-ui
.DS_Store
*.bak
.idea

# Local secrets (Figma token, etc.)
.env
.env.local
.env.*.local

# Local Figma API dumps (may contain design exports or tokens in paths — keep out of git)
fig_*.json

# Ensure these files are tracked
!CLAUDE.md
!AGENTS.md
